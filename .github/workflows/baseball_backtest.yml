name: Baseball Backtest (Legacy - manual only)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  legacy-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Explain production workflow
        run: |
          echo 'The legacy continuous workflow is intentionally manual-only.'
          echo 'Use .github/workflows/baseball_production.yml for the production NPB + MLB pipeline.'

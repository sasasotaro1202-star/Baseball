name: Baseball Source Acquisition (Legacy Manual)
on:
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: baseball-source-acquisition
  cancel-in-progress: false
jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy sequential acquisition retained for manual recovery only; scheduled acquisition uses parallel lanes."

# Time-budget policy: production jobs intentionally run in resumable 60-minute slices.
# Checkpoints/artifacts are persisted by the existing persist job so later runs
# continue from the saved state instead of requiring a single long execution.
